def print_words_separately(sentence)
  words = sentence.split(" ")

  words.each do |word|
    puts word
  end
end

# Пример использования
sentence = "Это пример предложения, которое будет разделено на отдельные слова"
print_words_separately(sentence)
